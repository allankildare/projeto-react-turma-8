import styled from "styled-components"

export const StyledButton = styled.button`
    background-color: var(--red);
    color: var(--color);
    font-size: 18px;
    font-weight: 700;
    padding: 8px 10px;
    border-radius: 5px;
    border: none;
    cursor: pointer;
`